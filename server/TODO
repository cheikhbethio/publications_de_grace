User ==> OK
  mise à jour du delete de user
  verif du findandUpdate si la proprieté n'est pas dans l'obj
  patch au lieu du put,
  u
Peom =>
  veruif userid avant le save(). est ce que le populate ne le fait pas déja?
  verif du findandUpdate si la proprieté n'est pas dans l'obj
  remplacement de update_at par updated_at
  l'or du udate il faut mettre  àjour le updated_at


general =>
      chacun doit pouvoir supprimer uniquement ses propres poemes.
      dans le update de databaseService replcer le _id par result (uniforme)


coherence
  nom des collection pas conforme (soit avec s ou sans s, maius pas les deux)

json-schema
  utiliser les valeur par defaut automatique dans le schéma
  pour les user supprimer le premier schema.
  mettre en place les min et max length
